# BUILD LAB v0.26 → v0.30 更新手順

## GitHub更新用ZIPを使う場合
1. 現在のv0.26フォルダをバックアップします。
2. `build_lab_update_v0.26_to_v0.30.zip` を展開します。
3. 展開された内容をv0.26のルートへ、フォルダ構造を維持したまま上書きします。
4. `index.html` を再読み込みします。ブラウザキャッシュが残る場合は強制再読み込みしてください。
5. 必要に応じて `tests/run_checks.bat` または `bash tests/run_checks.sh` を実行します。

## セーブデータ
v0.30は `build_lab_proto_v30` を使用し、v0.26以前のセーブを自動検出して移行します。旧キーは削除しないため、更新前バックアップとしても残ります。

## 主な更新内容
- 横断シナジー拡張（アルカナ/ルーン/元素/連結/特殊個体/複合挙動）
- 条件負荷監査と高条件ビルド再調整
- 解析カード・複合解析カード
- 二面カードを16種へ拡張
- 3段階成長カード6種とUID単位成長
- 支援遺物/プロトコル/調律追加

完全版を使う場合は、既存フォルダとは別の場所へ展開して起動してください。
